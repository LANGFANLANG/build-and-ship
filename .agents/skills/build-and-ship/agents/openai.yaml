interface:
  display_name: "Build and Ship"
  short_description: "端到端开发、验证和交付流程"
  default_prompt: "Use $build-and-ship to turn my app idea into a runnable, verified project."
policy:
  allow_implicit_invocation: true
